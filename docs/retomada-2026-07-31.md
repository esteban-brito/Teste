# Retomada operacional — carta canônica e próximas grandes etapas

> Estado registrado em **31/07/2026** para uma nova sessão sem memória da
> conversa. Leia primeiro `AGENTS.md`; depois este arquivo. Se o repositório
> tiver avançado, confirme `git status --short --branch` e `git log` antes de
> tratar hashes, contagens ou URLs abaixo como atuais.

## 1. Checkpoint publicado

- branch autorizada: `sandbox-test`;
- commit publicado ao fechar o ciclo visual: `7175c26`;
- `HEAD` e `origin/sandbox-test` estavam sincronizados e o worktree estava limpo;
- `main` permaneceu intocável;
- laboratório público:
  <https://esteban-brito.github.io/Teste/prototipo-cartas.html?cb=7175c26>;
- workflow de validação e Pages: execução `30652005186`, concluída com sucesso;
- o deploy serve o laboratório com `style.css?v=39c71a6e`, evitando que o
  navegador combine HTML novo com CSS antigo em cache;
- nenhum dado esportivo, OVR, role, playstyle, tier, configuração, snapshot,
  golden ou chamada de RNG foi alterado neste ciclo.

Os três últimos checkpoints explicam o fechamento:

| Commit | Responsabilidade |
|---|---|
| `876b15a` | alinhou a identidade frontal e devolveu largura/altura ao verso |
| `1e6452c` | tornou o nick compacto portável entre Windows e Linux |
| `7175c26` | versionou o CSS do laboratório no deploy do Pages |

## 2. Decisão visual encerrada

A **A refinada**, usando `donk_kato24` como referência, é a verdade absoluta da
carta de jogador. Não existe mais A/B/C, proposta paralela, comparador de
enquadramento ou layout especial por jogador. `device` também não é mais a
referência do laboratório.

### Frente

- a fotografia ocupa a área até a diagonal; não se aumenta sua presença com
  zoom individual;
- todo retrato é normalizado como asset 5:7 e o runtime usa sempre
  `background-size: 100% auto` e `background-position: 50% 12%`;
- a placa inferior mede 24% na carta ampla, 26% na intermediária e 28% na
  compacta;
- nick e bandeira compartilham a primeira linha e ficam centrados no mesmo eixo;
- a função principal ocupa a segunda linha;
- função secundária e time dividem a última linha, em extremos opostos;
- a bandeira não fica sobre o time e há uma separação mínima comprovada;
- todos os jogadores compartilham os mesmos corpos, tracking, margens e grade;
  não se aceita `zoom`, fonte, escala, offset ou CSS por nick;
- o nick usa `clamp(13px, 10.5cqw, 27px)` na grade normal; até 176 px recebe
  tracking universal de `-.025em`; até 150 px usa
  `clamp(12px, 10cqw, 27px)`;
- role principal, role secundário, bandeira e time continuam visíveis inclusive
  a 120 px.

### Verso

- nome, rótulo `Playstyle`, playstyle, quatro stats e rodapé são obrigatórios;
- os quatro stats ocupam toda a largura útil e pelo menos 35% da altura da carta;
- cada trilho usa 100% da largura do bloco, com distribuição vertical regular;
- campeonato + ano e colocação ficam no rodapé reservado, sem cair para fora ou
  concentrar todo o conteúdo no centro;
- o verso não exibe OVR, RTG nem os pesos numéricos internos da receita;
- os stats continuam derivados da receita real; remover informação visual não
  autoriza alterar os dados ou o motor.

### Treinadores

Treinador continua sendo outra categoria visual, mas usa o mesmo componente,
controle de frente/verso, rodapé de era, acessibilidade e sistema de eixos. Não
criar uma segunda infraestrutura de carta para ele.

## 3. Fontes de verdade e provas

- `style.css`: geometria e aparência canônicas;
- `src/ui/game/card-view.mjs`: HTML puro de frente e verso;
- `src/application/card-face.mjs`: única transição permitida entre faces;
- `prototipo-cartas.html`: laboratório fino que importa os artefatos reais;
- `bancada/e2e-cartas.js`: contrato executável do componente;
- `docs/card-portraits.md`: normalização e entrada dos retratos;
- `docs/cartas-design-2026-07-28.md`: histórico das decisões e rejeições.

O E2E mede **153 cartas reais e sintéticas**, incluindo os 85 jogadores, em
**250, 188, 176, 151, 150, 149, 130 e 120 px**. Ele reprova overflow, recorte,
colisão, conteúdo oculto, menos de quatro stats, largura incompleta dos trilhos,
ocupação vertical insuficiente, desalinhamento bandeira/nick, bandeira perto do
time, exceção tipográfica, fonte não carregada, contraste, regressão de teclado,
touch, reduced motion ou uso divergente no jogo real.

A comparação visual final mudou exatamente os nove estados que contêm cartas —
frente, verso e elenco em desktop, tablet e celular. Os outros 12 de 21 estados
permaneceram pixel a pixel idênticos. `npm run validate` fechou as 25 suítes.

Dois problemas já entendidos não devem ser reintroduzidos:

1. FreeType/Linux mede alguns nicks um pouco mais largos que DirectWrite/Windows;
   a solução é o tracking e o corpo compactos universais acima, não exceção por
   jogador;
2. GitHub Pages podia servir CSS antigo ao abrir o laboratório por uma URL nova;
   o workflow agora aplica cache-busting por conteúdo também ao CSS do protótipo.

## 4. Retratos: próxima fatia de produto recomendada

Hoje `donk_kato24` é o único retrato ligado ao dado cru, logo a cobertura é 1/85.
Fotos recebidas podem ter resolução, formato, qualidade, iluminação e recorte
diferentes: elas devem terminar em assets diferentes, mas em cartas idênticas.

A próxima entrega visível recomendada é completar um **lote coerente por time e
era**, começando pelos quatro jogadores restantes de Spirit · IEM Katowice 2024:
`sh1ro_kato24`, `zont1x`, `magixx` e `chopper_kato24`. Depois, repetir por time
até cobrir o acervo, em vez de misturar dezenas de origens numa única mudança
difícil de auditar.

O contrato executável atual de `foto` cobre `ATRIBUTOS`, isto é, jogadores. Uma
foto para o treinador `hally` exige antes uma pequena extensão explícita do dado,
catálogo, projeção pública e checker de assets; não se deve colocar um arquivo
órfão em `fotos/` nem improvisar caminho no HTML.

Para cada imagem:

1. confirmar jogador, ID cru, time e era em `src/data/catalog.mjs` e nas tabelas
   de dados;
2. registrar origem/licença no inventário do lote;
3. normalizar enquadramento, luz, ruído e fundo para 5:7 sem alterar a identidade;
4. exportar WebP opaco, preferencialmente 1000×1400 e no máximo 500 kB;
5. adicionar somente o asset-id ao dado cru;
6. rodar as guardas e capturar frente e verso em todas as oito larguras;
7. corrigir o asset quando o enquadramento falhar; nunca compensar em CSS.

Uma foto com cabeça ou ombros já cortados exige outra fonte ou reconstrução
apenas do fundo. Expansão generativa que invente rosto, uniforme, patrocinador ou
logo precisa de aprovação explícita.

## 5. Próximas grandes etapas recomendadas

Estas são prioridades recomendadas, não funcionalidades já implementadas:

| Ordem | Etapa | Resultado esperado | Dependência/risco |
|---|---|---|---|
| 1 | Biblioteca visual | retratos padronizados por time/era, com proveniência e QA | baixo risco esportivo; valor visual imediato |
| 2 | Encerrar P5 | extrair criação/reset de `S`, `TG`, `MP` e `MATCH`; depois controladores em commits separados | preservar identidade, timers e RNG |
| 3 | Pré-produção da Carreira | fechar fantasia, duração, progressão, transferências, dificuldade e entrada do modo; registrar ADR e fluxo de telas | decisões de produto do responsável |
| 4 | Fatia vertical da Carreira | criador de jogador + save versionado + uma temporada curta jogável usando os motores atuais | depende do estado da aplicação estar limpo |
| 5 | Profissionalização P6/P7 | retirar loader inline do calibrador, reduzir wiring residual e auditar órfãos | somente por paridade, sem balancear |
| paralela | Corpus IFCS | adquirir, selar e auditar 800 mapas/6 eventos; só então calcular nota oficial | longa, científica e não bloqueia o produto |
| sem data | Retorno e Desafio (Etapa 2) | Desafio Diário com seed do dia, streak e resultado compartilhável; Modo Profissional opt-in | plano já aprovado em `docs/memoria-e-retorno.md`; a Etapa 1 foi entregue |

**Backlog resgatado em 31/07/2026:** `docs/memoria-e-retorno.md` registra um plano
aprovado pelo responsável em 24/07 cuja **Etapa 1 (Memória, História e Hall) foi
entregue** — vive em `src/domain/narrative/game-memory.mjs`, no `PROGRESSO` e na
suíte `bancada/memoria.js`. A **Etapa 2 nunca entrou em nenhum roadmap** e ficou
invisível: nenhum outro documento a citava. Ela não tem data nem prioridade
atribuída; está listada aqui apenas para não se perder de novo. O Modo Profissional
inclui química punitiva, que é **balanceamento** e exige commit próprio com
comparação antes/depois.

### Recomendação de produto para a Carreira

O primeiro recorte deve ser pequeno e completo: criar um jogador a partir de
atributos crus, escolher uma identidade inicial, salvar/recarregar, disputar uma
temporada curta, receber progressão legível e chegar a um encerramento. Derivados
como role, playstyle e OVR devem sempre ser recalculados pelos motores existentes;
o save não deve persistir esses resultados como fonte de verdade.

Não começar a Carreira por mercado completo, dezenas de temporadas, narrativa
procedural extensa ou IA de transferências. Esses sistemas ganham sentido depois
que o loop mínimo provar que criar, jogar, evoluir e continuar um save é divertido.

### O que deve esperar

- reformulação ampla de roles e playstyles: é balanceamento estrutural, altera o
  snapshot dos 85 jogadores e precisa de evidência/medição própria;
- novo tuning do simulador: o ciclo atual está verde e sem critério vermelho;
- reescrita em framework: não resolve por si só estado, contratos ou produto;
- nota IFCS oficial antes do corpus completo;
- otimização sem benchmark de desempenho separado.

## 6. Sequência segura para a próxima sessão

```text
1. git status --short --branch
2. git log -8 --oneline --decorate
3. ler AGENTS.md e este handoff
4. ler o documento especializado da frente escolhida
5. confirmar o estado executável antes de editar
6. manter uma responsabilidade por commit
7. rodar a matriz de validação pertinente
8. confirmar git status, commit, push e CI/Pages
```

Para retratos/cartas, leia também `docs/card-portraits.md`,
`docs/cartas-design-2026-07-28.md` e `docs/testing.md`. Para estado/UI, leia
`docs/p5-aplicacao-ui-2026-07-29.md` inteiro. Para motor ou dados, comece por
`src/data/catalog.mjs` e siga as referências obrigatórias de `AGENTS.md`.

## 7. Definição de “não esquecer”

Uma sessão futura não deve gastar tempo redesenhando a carta A, recolocando a
bandeira perto do time, centralizando um bloco pequeno de stats, ampliando a foto
por zoom, criando exceções por jogador ou usando `device` como referência. O
trabalho visual agora é **conteúdo padronizado sobre o componente aprovado**.

Na arquitetura, a próxima fatia já está especificada: estado primeiro,
controladores depois. No produto, a próxima grande aposta é a Carreira, mas ela
deve nascer como uma fatia vertical pequena e verificável sobre os motores atuais.
