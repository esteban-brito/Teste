{
  "name": "sandbox",
  "private": true,
  "scripts": {
    "bench": "node bancada/run.js",
    "check": "node --check game.js && node tools/check-sandbox-engine.js && node tools/check-sandbox-syntax.js",
    "lint": "eslint game.js sandbox.html calibrator-worker.js bancada/*.js tools/*.js",
    "serve": "node tools/serve-static.js"
  },
  "devDependencies": {
    "eslint": "^10.6.0",
    "@eslint/js": "^10.0.1",
    "eslint-plugin-html": "^8.1.4"
  }
}
